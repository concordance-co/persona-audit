"""The demo dataset factory: the worked example that built the shipped demo.

Not part of the product runtime — backend/ never imports factory/ (enforced
by a hygiene test). See factory/README.md.
"""
